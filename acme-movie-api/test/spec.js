const { expect } = require("chai");

describe("Sample Test", () => {
  it("should equal 2", () => {
    const example = 1 + 1;
    expect(example).to.equal(2);
  });
  it("should equal 2", () => {
    const example = 1 + 1;
    expect(example).to.equal(2);
  });
});
